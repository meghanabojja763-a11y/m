# 🧠 Advanced Image Matching App

A Streamlit-based web app for image similarity detection using:

- Image enhancement + morphological processing
- ORB feature matching
- SSIM pixel similarity
- Deep Learning (ResNet-50) feature embedding

## 🚀 How to Run Locally

```bash
git clone https://github.com/<your-username>/image-matching-app.git
cd image-matching-app
pip install -r requirements.txt
streamlit run app.py
